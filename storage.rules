rules_version = '2';

// The app does not allow direct browser uploads to Firebase Storage.
// FastAPI handles authenticated /photo-uploads requests and writes to
// the private Cloud Storage bucket through Cloud Run IAM.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
